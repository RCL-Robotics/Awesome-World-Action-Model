{
  "name": "awesome-world-action-model",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22.12.0" },
  "scripts": {
    "dev": "astro dev --host 127.0.0.1",
    "build": "npm run validate:data && astro build",
    "preview": "astro preview --host 127.0.0.1",
    "check": "astro check",
    "test": "node --test tests/*.test.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "sync:notion": "node scripts/sync-notion.mjs",
    "generate:readme": "node scripts/generate-readme.mjs"
  },
  "dependencies": { "astro": "7.3.1" },
  "devDependencies": { "@astrojs/check": "0.9.10", "typescript": "5.9.3" }
}
