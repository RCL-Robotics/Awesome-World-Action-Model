Describe the problem and the resulting behavior. For visual changes, include a screenshot at desktop and mobile widths.

## Validation

- [ ] `npm run check`
- [ ] `npm test`
- [ ] `npm run build`

For paper additions or metadata corrections, link the relevant Issue and ensure the change is reflected in Notion before the next export. Regenerate the README with `npm run generate:readme`.
