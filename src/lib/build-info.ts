// Captured during static generation and shipped with the website artifact.
// This is independent of bibliography updates and the reader's browser clock.
export const websiteUpdatedAt = new Date().toISOString();
export const websiteUpdatedLabel = `${websiteUpdatedAt.slice(0, 16).replace('T', ' ')} UTC`;
