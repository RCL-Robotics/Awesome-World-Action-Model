---
import { readingStatusLabel, type ReadingStatus } from '../../lib/reports';
interface Props { status: ReadingStatus; }
const { status } = Astro.props;
---
<span class="rn-status" data-status={status}><span aria-hidden="true"></span>{readingStatusLabel[status]}</span>
