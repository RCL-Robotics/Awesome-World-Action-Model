<!-- Apply the saved size before the page is painted. -->
<script is:inline>
  try {
    document.documentElement.dataset.fontSize = localStorage.getItem('wam-font-size') === 'small' ? 'small' : 'large';
  } catch {
    document.documentElement.dataset.fontSize = 'large';
  }
</script>
